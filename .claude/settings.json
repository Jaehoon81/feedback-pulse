{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c \"if [ \\\"$HARNESS_MODE\\\" = \\\"1\\\" ]; then echo 'skip: harness sub-session (lint/build/test runs at phase finalize)'; elif [ -f package.json ]; then npm run lint && npm run build && npm run test; else echo 'skip: no package.json yet'; fi\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python scripts/check_dangerous_command.py"
          }
        ]
      }
    ]
  }
}
